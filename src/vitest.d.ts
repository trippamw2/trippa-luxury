/// <reference types="vitest/globals" />

/**
 * Ambient Vitest globals so `tsc --noEmit` understands `describe`, `it`,
 * and `expect` in co-located `*.test.ts` files. Vitest itself runs tests
 * independently of these type definitions.
 */
